run_name: "500_sparse2"
data:
  num_patients: 128
  test_fraction: 0.25
  batch_size: 8
  seed: 91
  device: cpu
  beta_t_active_frac: 0.3
  beta_t_active_scale: 2.0
model:
  n_subtypes: 3
  n_cell_types: 8
  n_genes: 500
  z_dim: 8
  u_dim: 8
  beta_t_l1_strength: 0.1
  subtype_concentration: 2.0
  subtype_rate: 0.1
  nb_dispersion_prior: 2.0
  nb_dispersion_rate: 1.0
vi:
  early_stopping_patience: 40
  learning_rate: 0.0008
  num_epochs: 500
  log_interval: 100
  guide: auto_normal
